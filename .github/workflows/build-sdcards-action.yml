name: Build Codes and SDCards

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
#    - uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install build-essential python3-dev python3-pip python3-setuptools
        sudo apt-get update
#        sudo apt-get install devkitpro
#        wget https://apt.devkitpro.org/install-devkitpro-pacman
#        chmod +x ./install-devkitpro-pacman
#        sudo ./install-devkitpro-pacman

    - name: Install devkitPPC
      run: |
        wget https://apt.devkitpro.org/install-devkitpro-pacman
        chmod +x ./install-devkitpro-pacman
        yes | sudo ./install-devkitpro-pacman
        sudo chmod +x ./install-devkitpro-pacman
        sudo ./install-devkitpro-pacman
        sudo dkp-pacman -Syu
        sudo dkp-pacman -S devkitPPC libogc libfat

    - name: Verify installation
      run: |
        echo 'int main(){}' > main.c
        powerpc-eabi-gcc -mcpu=750 -mhard-float main.c -o main.elf

    - name: Config and Build Codes
      run: sh ./build-linux.sh
